#!/usr/bin/env bash
# Wrapper when `pip install -e .` did not put hier-walk on PATH (common on corp PCs).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
eval "$(python3 "$ROOT/bootstrap_path.py" --export --anchor "$0")"
exec python3 -m hierwalk "$@"